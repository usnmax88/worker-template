# ---------- CUDA wheels ----------
--extra-index-url https://download.pytorch.org/whl/cu121

# ---------- Runtime / orchestration ----------
runpod>=1.6,<2.0

# ---------- PyTorch core (CUDA 12.1) ----------
# Keep these three in lockstep with Torch 2.4.0 CUDA12.1 wheels
torch==2.4.0
torchvision==0.19.0
torchaudio==2.4.0

# ---------- Core utilities (REQUIRED) ----------
numpy>=1.26,<3
pillow>=10,<11
requests>=2.31,<3
tqdm>=4.66,<5
packaging>=23.0

# ---------- Model + I/O plumbing (REQUIRED for OmniAvatar) ----------
safetensors>=0.4.0
imageio>=2.31.0
scipy>=1.14.0
PyYAML>=6.0
einops>=0.6.0

# ---------- Hugging Face stack (REQUIRED) ----------
transformers>=4.52.3
tokenizers>=0.15.0
huggingface-hub>=0.20.0
hf_transfer>=0.1.0     # moved to REQUIRED because your installs fail without it

# ---------- OmniAvatar-specific (REQUIRED) ----------
xfuser>=0.4.1

# ---------- Audio pipeline (REQUIRED if you drive from audio) ----------
librosa>=0.10.0
soundfile>=0.12.0

# ---------- Helpful but OPTIONAL ----------
accelerate>=0.20.0     # multi-GPU / device placement helper

# ---------- Fine-tune utils (OPTIONAL; not needed for plain inference) ----------
peft>=0.15.1

# ---------- Text cleanup (OPTIONAL) ----------
ftfy>=6.0.0
